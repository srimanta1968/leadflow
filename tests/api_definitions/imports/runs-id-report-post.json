{
  "name": "POST /api/leadflow/imports/runs/{run_id}/report - the completed-run report",
  "description": "Builds the report behind the Report action on a finished run: what was read, what was committed, what was excepted, what it created, which governance checks ran, and whether the rollback window is still open.\n\nPOST AND 200, DELIBERATELY, and neither is an accident. It is a COMMAND that composes a report - it creates no addressable resource, so 201 would be a lie about a Location nobody can follow (MUST-54). It is a POST rather than a GET because composing it fans out across sdk-import for the run, its lineage and its exceptions, and because it is recorded as a disclosure: reports get forwarded, and an import report names what happened to whose data.\n\nROLLBACK AVAILABILITY IS COMPUTED, NOT COPIED. The report states whether the window is STILL OPEN as of now, because 'rollback_deadline: last Tuesday' asks the reader to do date arithmetic to answer the only question they actually have.\n\nDEGRADES TO AN HONEST 200, NEVER A 502. When ProjexCloud is not configured the response carries upstream_available:false and empty collections rather than an error page - the same choice the Capture Inbox makes, and for the same reason: a screen that renders nothing and says why leaves the operator able to use the rest of the product, and that matters most during exactly the outage that produces it. This is what the suite observes in dev, so the happy path asserts success:true rather than a populated body.\n\nrequiresRole:[\"data_steward\"] and NO testCase pins an Authorization header. import.run_read is held by the Data Steward and the Privacy Officer; the suite's default identity bridges to sales_rep and would only ever see 403. The runner swaps to testCredentials.data_steward (qa.steward@leadflow.test, local role `steward`). A pinned header would take PRECEDENCE over that token and defeat the swap (MUST-71), which is why the 401 cases are documented in errorCases rather than authored - they are middleware behaviour identical across every route and are covered by the definitions that declare no role.\n\nWHAT THE SUITE OBSERVES IS THE 404, and expectedStatus states that rather than an aspirational 200. No LeadFlow route creates an import run - runs are created in sdk-import, and the Import wizard drives it directly - so there is no producer to chain from and no seeded run to read. The id therefore comes from {{var:absent_import_run_id}} and the assertion is the not-found branch, which is a REAL branch worth holding: it is the one that proves an upstream 404 is reported as a missing run rather than as an outage. Point the variable at a seeded run in an environment that has one and the same call returns the populated 200 the description above describes.",
  "endpoint": "/api/leadflow/imports/runs/{run_id}/report",
  "method": "POST",
  "taskId": "16e54e8c-590d-41d1-9494-9249519c107f",
  "sprintId": "cd0927d4-40eb-46b2-b4e0-33edc043db67",
  "requiresAuth": true,
  "requiresRole": [
    "data_steward"
  ],
  "testability": "auto",
  "priority": 10,
  "expectedStatus": 404,
  "coversCriterion": [
    "16e54e8c-590d-41d1-9494-9249519c107f#AC2",
    "16e54e8c-590d-41d1-9494-9249519c107f#AC4"
  ],
  "dependsOn": [
    "POST /api/auth/register",
    "POST /api/auth/login"
  ],
  "testCases": [
    {
      "name": "NEGATIVE: a well-formed run id that sdk-import reports as absent",
      "pathParams": {
        "run_id": "{{var:absent_import_run_id}}"
      },
      "payload": {},
      "expectedStatus": 404,
      "expectedResponse": {
        "success": false,
        "code": "NOT_FOUND"
      },
      "testType": "negative",
      "coversErrorCase": "404|NOT_FOUND"
    }
  ],
  "errorCases": [
    {
      "status": 401,
      "code": "UNAUTHENTICATED",
      "message": "Authorization header with a bearer token is required",
      "when": "the Authorization header is absent, empty, or uses a scheme other than Bearer. Not authored as a testCase: it can only be expressed by pinning a header, which would defeat the role swap (MUST-71)."
    },
    {
      "status": 401,
      "code": "INVALID_TOKEN",
      "message": "Session token is invalid or expired",
      "when": "a Bearer token was presented but does not verify. Not authored, for the same reason as above."
    },
    {
      "status": 403,
      "code": "FORBIDDEN",
      "message": "No policy grants this action to the caller's roles.",
      "when": "the caller's roles do not grant import.run_read, which is held by the Data Steward and the Privacy Officer. NOT reachable while this definition runs under requiresRole, and kept because it is a real branch the default suite identity receives."
    },
    {
      "status": 404,
      "code": "NOT_FOUND",
      "message": "No import run with that id",
      "when": "sdk-import is reachable and reports no such run. NOT reachable in an environment with no gateway configured, where the endpoint answers 200 with upstream_available:false instead - which is exactly the distinction these endpoints preserve rather than collapse."
    }
  ]
}
