{
  "name": "GET /api/leadflow/imports/runs/{run_id}/evidence - attestation and evidence bundle",
  "description": "The Evidence action on a restricted third-party run: the rights attestation sworn when the data was brought in, the permitted-use answer that attestation supports, and the governance verdicts recorded against it.\n\nA NARROWER GRANT THAN THE REST OF THE SCREEN, which is the whole reason this endpoint is separate. It is gated on import.evidence_read, held by the Data Steward and the Privacy Officer and by nobody else - the one permission those two roles share. Everything else in this feature is import.run_read, which is deliberately wider: many people may watch imports run, far fewer may read who swore the data was lawfully obtained and on what basis. Folding this into the run detail would have handed that material to every viewer of the register.\n\nAUDITED AS A DISCLOSURE under its own event, import.evidence.exported.v1, rather than under the event an ordinary run read uses. 'Somebody looked at the import queue' and 'somebody read the attestation' are different disclosures, and an audit trail that spells them identically cannot answer the second question - which is the one a complaint actually asks.\n\nDEGRADES TO AN HONEST 200, NEVER A 502. When ProjexCloud is not configured the response carries upstream_available:false and empty collections rather than an error page - the same choice the Capture Inbox makes, and for the same reason: a screen that renders nothing and says why leaves the operator able to use the rest of the product, and that matters most during exactly the outage that produces it. This is what the suite observes in dev, so the happy path asserts success:true rather than a populated body.\n\nrequiresRole:[\"data_steward\"] for the same reason as its siblings, and NO testCase pins an Authorization header (MUST-71). A Privacy Officer reaches it equally; the steward is simply the identity the runner has seeded.\n\nWHAT THE SUITE OBSERVES IS THE 404, and expectedStatus states that rather than an aspirational 200. No LeadFlow route creates an import run - runs are created in sdk-import, and the Import wizard drives it directly - so there is no producer to chain from and no seeded run to read. The id therefore comes from {{var:absent_import_run_id}} and the assertion is the not-found branch, which is a REAL branch worth holding: it is the one that proves an upstream 404 is reported as a missing run rather than as an outage. Point the variable at a seeded run in an environment that has one and the same call returns the populated 200 the description above describes.",
  "endpoint": "/api/leadflow/imports/runs/:run_id/evidence",
  "method": "GET",
  "taskId": "16e54e8c-590d-41d1-9494-9249519c107f",
  "featureId": "01724402-1fc2-45e4-90e4-f6c46286286f",
  "epicId": "8eca7fd1-6541-4863-81ec-bf170938403d",
  "sprintId": "cd0927d4-40eb-46b2-b4e0-33edc043db67",
  "requiresAuth": true,
  "requiresRole": [
    "data_steward"
  ],
  "testability": "auto",
  "priority": 10,
  "expectedStatus": 404,
  "coversCriterion": [
    "16e54e8c-590d-41d1-9494-9249519c107f#AC3",
    "16e54e8c-590d-41d1-9494-9249519c107f#AC4"
  ],
  "dependsOn": [
    "POST /api/auth/register",
    "POST /api/auth/login"
  ],
  "testCases": [
    {
      "name": "NEGATIVE: a well-formed run id that sdk-import reports as absent",
      "coversCriterion": "16e54e8c-590d-41d1-9494-9249519c107f#AC3",
      "pathParams": {
        "run_id": "{{var:absent_import_run_id}}"
      },
      "expectedStatus": 404,
      "expectedResponse": {
        "success": false,
        "code": "NOT_FOUND"
      },
      "testType": "negative",
      "coversErrorCase": "404|NOT_FOUND"
    }
  ],
  "errorCases": [
    {
      "status": 401,
      "code": "UNAUTHENTICATED",
      "message": "Authorization header with a bearer token is required",
      "when": "the Authorization header is absent, empty, or uses a scheme other than Bearer. Not authored as a testCase: it can only be expressed by pinning a header, which would defeat the role swap (MUST-71)."
    },
    {
      "status": 401,
      "code": "INVALID_TOKEN",
      "message": "Session token is invalid or expired",
      "when": "a Bearer token was presented but does not verify. Not authored, for the same reason as above."
    },
    {
      "status": 403,
      "code": "FORBIDDEN",
      "message": "No policy grants this action to the caller's roles.",
      "when": "the caller's roles do not grant import.evidence_read, which is held by the Data Steward and the Privacy Officer, and no other role. NOT reachable while this definition runs under requiresRole, and kept because it is a real branch the default suite identity receives."
    },
    {
      "status": 404,
      "code": "NOT_FOUND",
      "message": "No import run with that id",
      "when": "sdk-import is reachable and reports no such run. NOT reachable in an environment with no gateway configured, where the endpoint answers 200 with upstream_available:false instead - which is exactly the distinction these endpoints preserve rather than collapse."
    }
  ]
}
