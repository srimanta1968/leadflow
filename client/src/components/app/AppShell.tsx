/**
 * Moved to design-system/shell.
 *
 * Re-exported here so existing imports keep working while the shell lives with
 * the tokens it consumes. Deleting this file outright would have been a rename
 * disguised as a refactor, and the MUST-32 gate is right to treat that as a
 * dangling reference.
 */
export { AppShell } from '../../design-system/shell/AppShell';
