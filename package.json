{
  "name": "leadflow-workspace",
  "private": true,
  "version": "1.0.0",
  "description": "Root launcher for the LeadFlow client and server. Runs both dev servers with one command.",
  "scripts": {
    "predev": "node scripts/check-ports.js",
    "dev": "concurrently --names server,client --prefix-colors cyan,magenta --kill-others-on-fail \"npm:dev:server\" \"npm:dev:client\"",
    "ports": "node scripts/check-ports.js",
    "dev:server": "npm --prefix server run dev",
    "dev:client": "npm --prefix client run dev",
    "dev:sdk-cloud": "cross-env PROJEXCLOUD_GATEWAY_URL=https://cloud.projexlight.com npm run dev",
    "dev:sdk-local": "cross-env PROJEXCLOUD_GATEWAY_URL=http://localhost:3500 npm run dev",
    "install:all": "npm install && npm --prefix server install && npm --prefix client install",
    "build": "npm --prefix server run build && npm --prefix client run build",
    "start": "npm --prefix server run start",
    "typecheck": "npm --prefix server run typecheck && npm --prefix client run typecheck",
    "test": "npm --prefix server run test && npm --prefix client run test -- --run",
    "test:server": "npm --prefix server run test",
    "test:client": "npm --prefix client run test -- --run",
    "lint": "npm --prefix server run lint && npm --prefix client run lint"
  },
  "devDependencies": {
    "concurrently": "^9.1.0",
    "cross-env": "^7.0.3"
  },
  "engines": {
    "node": ">=20"
  }
}
